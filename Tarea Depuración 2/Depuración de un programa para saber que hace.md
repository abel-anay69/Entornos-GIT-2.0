# Tarea 2: Depuración de un programa para saber qué hace.

#### 1. Explicar qué hace el método main.

*En dos Arrys, la maquina asignara números aleatorios que luego ordenara de mayor a menor y nos mostrara el tiempo en milisengundos que ha tardado en hacer cada una de las dos listas.*
github
#### 2. Poner un punto de ruptura (breakpoint) en la línea 78 (primer bucle del método intercambio) y, basándoos en los valores que van tomando las variables, explicad cómo funciona el método de ordenación de arrays por intercambio. Podéis crear tablas para ver cómo cambian los valores de los arrays.

*Comprueba los valores aleatorios para ver que no son mayoes o menores si es mayor no se mueve pero si es menor lo movera a la primera posicion (dependiendo si el valor minimo ya esta en la primera posicion), despues volvera a comprobar los valores para que se vayan intercambiando los valores como corresponde.*

#### 3. Poner un punto de ruptura (breakpoint) en la línea 94 y explicad cómo funciona el método de ordenación de arrays de conteo. Podéis crear tablas para ver cómo cambian los valores de los arrays.

*Comprueba los valores si coinciden le resta a la i para luego sumarle a la z y para que se igualen tendra que restarle uno al contador*

